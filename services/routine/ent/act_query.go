// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"routine/ent/act"
	"routine/ent/predicate"
	"routine/ent/routineact"
	"routine/ent/routineactrec"
	"routine/ent/tag"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ActQuery is the builder for querying Act entities.
type ActQuery struct {
	config
	ctx                *QueryContext
	order              []act.OrderOption
	inters             []Interceptor
	predicates         []predicate.Act
	withTags           *TagQuery
	withRoutineActs    *RoutineActQuery
	withRoutineActRecs *RoutineActRecQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ActQuery builder.
func (aq *ActQuery) Where(ps ...predicate.Act) *ActQuery {
	aq.predicates = append(aq.predicates, ps...)
	return aq
}

// Limit the number of records to be returned by this query.
func (aq *ActQuery) Limit(limit int) *ActQuery {
	aq.ctx.Limit = &limit
	return aq
}

// Offset to start from.
func (aq *ActQuery) Offset(offset int) *ActQuery {
	aq.ctx.Offset = &offset
	return aq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aq *ActQuery) Unique(unique bool) *ActQuery {
	aq.ctx.Unique = &unique
	return aq
}

// Order specifies how the records should be ordered.
func (aq *ActQuery) Order(o ...act.OrderOption) *ActQuery {
	aq.order = append(aq.order, o...)
	return aq
}

// QueryTags chains the current query on the "tags" edge.
func (aq *ActQuery) QueryTags() *TagQuery {
	query := (&TagClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(act.Table, act.FieldID, selector),
			sqlgraph.To(tag.Table, tag.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, act.TagsTable, act.TagsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRoutineActs chains the current query on the "routine_acts" edge.
func (aq *ActQuery) QueryRoutineActs() *RoutineActQuery {
	query := (&RoutineActClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(act.Table, act.FieldID, selector),
			sqlgraph.To(routineact.Table, routineact.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, act.RoutineActsTable, act.RoutineActsColumn),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRoutineActRecs chains the current query on the "routine_act_recs" edge.
func (aq *ActQuery) QueryRoutineActRecs() *RoutineActRecQuery {
	query := (&RoutineActRecClient{config: aq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(act.Table, act.FieldID, selector),
			sqlgraph.To(routineactrec.Table, routineactrec.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, act.RoutineActRecsTable, act.RoutineActRecsColumn),
		)
		fromU = sqlgraph.SetNeighbors(aq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Act entity from the query.
// Returns a *NotFoundError when no Act was found.
func (aq *ActQuery) First(ctx context.Context) (*Act, error) {
	nodes, err := aq.Limit(1).All(setContextOp(ctx, aq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{act.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aq *ActQuery) FirstX(ctx context.Context) *Act {
	node, err := aq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Act ID from the query.
// Returns a *NotFoundError when no Act ID was found.
func (aq *ActQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = aq.Limit(1).IDs(setContextOp(ctx, aq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{act.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aq *ActQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := aq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Act entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Act entity is found.
// Returns a *NotFoundError when no Act entities are found.
func (aq *ActQuery) Only(ctx context.Context) (*Act, error) {
	nodes, err := aq.Limit(2).All(setContextOp(ctx, aq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{act.Label}
	default:
		return nil, &NotSingularError{act.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aq *ActQuery) OnlyX(ctx context.Context) *Act {
	node, err := aq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Act ID in the query.
// Returns a *NotSingularError when more than one Act ID is found.
// Returns a *NotFoundError when no entities are found.
func (aq *ActQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = aq.Limit(2).IDs(setContextOp(ctx, aq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{act.Label}
	default:
		err = &NotSingularError{act.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aq *ActQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := aq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Acts.
func (aq *ActQuery) All(ctx context.Context) ([]*Act, error) {
	ctx = setContextOp(ctx, aq.ctx, "All")
	if err := aq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Act, *ActQuery]()
	return withInterceptors[[]*Act](ctx, aq, qr, aq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aq *ActQuery) AllX(ctx context.Context) []*Act {
	nodes, err := aq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Act IDs.
func (aq *ActQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if aq.ctx.Unique == nil && aq.path != nil {
		aq.Unique(true)
	}
	ctx = setContextOp(ctx, aq.ctx, "IDs")
	if err = aq.Select(act.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aq *ActQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := aq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aq *ActQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aq.ctx, "Count")
	if err := aq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aq, querierCount[*ActQuery](), aq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aq *ActQuery) CountX(ctx context.Context) int {
	count, err := aq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aq *ActQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aq.ctx, "Exist")
	switch _, err := aq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aq *ActQuery) ExistX(ctx context.Context) bool {
	exist, err := aq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ActQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aq *ActQuery) Clone() *ActQuery {
	if aq == nil {
		return nil
	}
	return &ActQuery{
		config:             aq.config,
		ctx:                aq.ctx.Clone(),
		order:              append([]act.OrderOption{}, aq.order...),
		inters:             append([]Interceptor{}, aq.inters...),
		predicates:         append([]predicate.Act{}, aq.predicates...),
		withTags:           aq.withTags.Clone(),
		withRoutineActs:    aq.withRoutineActs.Clone(),
		withRoutineActRecs: aq.withRoutineActRecs.Clone(),
		// clone intermediate query.
		sql:  aq.sql.Clone(),
		path: aq.path,
	}
}

// WithTags tells the query-builder to eager-load the nodes that are connected to
// the "tags" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *ActQuery) WithTags(opts ...func(*TagQuery)) *ActQuery {
	query := (&TagClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withTags = query
	return aq
}

// WithRoutineActs tells the query-builder to eager-load the nodes that are connected to
// the "routine_acts" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *ActQuery) WithRoutineActs(opts ...func(*RoutineActQuery)) *ActQuery {
	query := (&RoutineActClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withRoutineActs = query
	return aq
}

// WithRoutineActRecs tells the query-builder to eager-load the nodes that are connected to
// the "routine_act_recs" edge. The optional arguments are used to configure the query builder of the edge.
func (aq *ActQuery) WithRoutineActRecs(opts ...func(*RoutineActRecQuery)) *ActQuery {
	query := (&RoutineActRecClient{config: aq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aq.withRoutineActRecs = query
	return aq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Act.Query().
//		GroupBy(act.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aq *ActQuery) GroupBy(field string, fields ...string) *ActGroupBy {
	aq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ActGroupBy{build: aq}
	grbuild.flds = &aq.ctx.Fields
	grbuild.label = act.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Act.Query().
//		Select(act.FieldName).
//		Scan(ctx, &v)
func (aq *ActQuery) Select(fields ...string) *ActSelect {
	aq.ctx.Fields = append(aq.ctx.Fields, fields...)
	sbuild := &ActSelect{ActQuery: aq}
	sbuild.label = act.Label
	sbuild.flds, sbuild.scan = &aq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ActSelect configured with the given aggregations.
func (aq *ActQuery) Aggregate(fns ...AggregateFunc) *ActSelect {
	return aq.Select().Aggregate(fns...)
}

func (aq *ActQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aq); err != nil {
				return err
			}
		}
	}
	for _, f := range aq.ctx.Fields {
		if !act.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aq.path != nil {
		prev, err := aq.path(ctx)
		if err != nil {
			return err
		}
		aq.sql = prev
	}
	return nil
}

func (aq *ActQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Act, error) {
	var (
		nodes       = []*Act{}
		_spec       = aq.querySpec()
		loadedTypes = [3]bool{
			aq.withTags != nil,
			aq.withRoutineActs != nil,
			aq.withRoutineActRecs != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Act).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Act{config: aq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aq.withTags; query != nil {
		if err := aq.loadTags(ctx, query, nodes,
			func(n *Act) { n.Edges.Tags = []*Tag{} },
			func(n *Act, e *Tag) { n.Edges.Tags = append(n.Edges.Tags, e) }); err != nil {
			return nil, err
		}
	}
	if query := aq.withRoutineActs; query != nil {
		if err := aq.loadRoutineActs(ctx, query, nodes,
			func(n *Act) { n.Edges.RoutineActs = []*RoutineAct{} },
			func(n *Act, e *RoutineAct) { n.Edges.RoutineActs = append(n.Edges.RoutineActs, e) }); err != nil {
			return nil, err
		}
	}
	if query := aq.withRoutineActRecs; query != nil {
		if err := aq.loadRoutineActRecs(ctx, query, nodes,
			func(n *Act) { n.Edges.RoutineActRecs = []*RoutineActRec{} },
			func(n *Act, e *RoutineActRec) { n.Edges.RoutineActRecs = append(n.Edges.RoutineActRecs, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aq *ActQuery) loadTags(ctx context.Context, query *TagQuery, nodes []*Act, init func(*Act), assign func(*Act, *Tag)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uint64]*Act)
	nids := make(map[uint64]map[*Act]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(act.TagsTable)
		s.Join(joinT).On(s.C(tag.FieldID), joinT.C(act.TagsPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(act.TagsPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(act.TagsPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := uint64(values[0].(*sql.NullInt64).Int64)
				inValue := uint64(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*Act]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Tag](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "tags" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (aq *ActQuery) loadRoutineActs(ctx context.Context, query *RoutineActQuery, nodes []*Act, init func(*Act), assign func(*Act, *RoutineAct)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*Act)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.RoutineAct(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(act.RoutineActsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.act_routine_acts
		if fk == nil {
			return fmt.Errorf(`foreign-key "act_routine_acts" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "act_routine_acts" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (aq *ActQuery) loadRoutineActRecs(ctx context.Context, query *RoutineActRecQuery, nodes []*Act, init func(*Act), assign func(*Act, *RoutineActRec)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*Act)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.RoutineActRec(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(act.RoutineActRecsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.act_routine_act_recs
		if fk == nil {
			return fmt.Errorf(`foreign-key "act_routine_act_recs" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "act_routine_act_recs" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (aq *ActQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aq.querySpec()
	_spec.Node.Columns = aq.ctx.Fields
	if len(aq.ctx.Fields) > 0 {
		_spec.Unique = aq.ctx.Unique != nil && *aq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aq.driver, _spec)
}

func (aq *ActQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(act.Table, act.Columns, sqlgraph.NewFieldSpec(act.FieldID, field.TypeUint64))
	_spec.From = aq.sql
	if unique := aq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aq.path != nil {
		_spec.Unique = true
	}
	if fields := aq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, act.FieldID)
		for i := range fields {
			if fields[i] != act.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aq *ActQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aq.driver.Dialect())
	t1 := builder.Table(act.Table)
	columns := aq.ctx.Fields
	if len(columns) == 0 {
		columns = act.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aq.sql != nil {
		selector = aq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aq.ctx.Unique != nil && *aq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range aq.predicates {
		p(selector)
	}
	for _, p := range aq.order {
		p(selector)
	}
	if offset := aq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ActGroupBy is the group-by builder for Act entities.
type ActGroupBy struct {
	selector
	build *ActQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (agb *ActGroupBy) Aggregate(fns ...AggregateFunc) *ActGroupBy {
	agb.fns = append(agb.fns, fns...)
	return agb
}

// Scan applies the selector query and scans the result into the given value.
func (agb *ActGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, agb.build.ctx, "GroupBy")
	if err := agb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActQuery, *ActGroupBy](ctx, agb.build, agb, agb.build.inters, v)
}

func (agb *ActGroupBy) sqlScan(ctx context.Context, root *ActQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(agb.fns))
	for _, fn := range agb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*agb.flds)+len(agb.fns))
		for _, f := range *agb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*agb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := agb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ActSelect is the builder for selecting fields of Act entities.
type ActSelect struct {
	*ActQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (as *ActSelect) Aggregate(fns ...AggregateFunc) *ActSelect {
	as.fns = append(as.fns, fns...)
	return as
}

// Scan applies the selector query and scans the result into the given value.
func (as *ActSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, as.ctx, "Select")
	if err := as.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActQuery, *ActSelect](ctx, as.ActQuery, as, as.inters, v)
}

func (as *ActSelect) sqlScan(ctx context.Context, root *ActQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(as.fns))
	for _, fn := range as.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*as.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := as.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
