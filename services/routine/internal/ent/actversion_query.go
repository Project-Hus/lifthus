// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"routine/internal/ent/act"
	"routine/internal/ent/actimage"
	"routine/internal/ent/actversion"
	"routine/internal/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ActVersionQuery is the builder for querying ActVersion entities.
type ActVersionQuery struct {
	config
	ctx           *QueryContext
	order         []actversion.OrderOption
	inters        []Interceptor
	predicates    []predicate.ActVersion
	withAct       *ActQuery
	withActImages *ActImageQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ActVersionQuery builder.
func (avq *ActVersionQuery) Where(ps ...predicate.ActVersion) *ActVersionQuery {
	avq.predicates = append(avq.predicates, ps...)
	return avq
}

// Limit the number of records to be returned by this query.
func (avq *ActVersionQuery) Limit(limit int) *ActVersionQuery {
	avq.ctx.Limit = &limit
	return avq
}

// Offset to start from.
func (avq *ActVersionQuery) Offset(offset int) *ActVersionQuery {
	avq.ctx.Offset = &offset
	return avq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (avq *ActVersionQuery) Unique(unique bool) *ActVersionQuery {
	avq.ctx.Unique = &unique
	return avq
}

// Order specifies how the records should be ordered.
func (avq *ActVersionQuery) Order(o ...actversion.OrderOption) *ActVersionQuery {
	avq.order = append(avq.order, o...)
	return avq
}

// QueryAct chains the current query on the "act" edge.
func (avq *ActVersionQuery) QueryAct() *ActQuery {
	query := (&ActClient{config: avq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := avq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := avq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(actversion.Table, actversion.FieldID, selector),
			sqlgraph.To(act.Table, act.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, actversion.ActTable, actversion.ActColumn),
		)
		fromU = sqlgraph.SetNeighbors(avq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryActImages chains the current query on the "act_images" edge.
func (avq *ActVersionQuery) QueryActImages() *ActImageQuery {
	query := (&ActImageClient{config: avq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := avq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := avq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(actversion.Table, actversion.FieldID, selector),
			sqlgraph.To(actimage.Table, actimage.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, actversion.ActImagesTable, actversion.ActImagesColumn),
		)
		fromU = sqlgraph.SetNeighbors(avq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ActVersion entity from the query.
// Returns a *NotFoundError when no ActVersion was found.
func (avq *ActVersionQuery) First(ctx context.Context) (*ActVersion, error) {
	nodes, err := avq.Limit(1).All(setContextOp(ctx, avq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{actversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (avq *ActVersionQuery) FirstX(ctx context.Context) *ActVersion {
	node, err := avq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ActVersion ID from the query.
// Returns a *NotFoundError when no ActVersion ID was found.
func (avq *ActVersionQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = avq.Limit(1).IDs(setContextOp(ctx, avq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{actversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (avq *ActVersionQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := avq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ActVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ActVersion entity is found.
// Returns a *NotFoundError when no ActVersion entities are found.
func (avq *ActVersionQuery) Only(ctx context.Context) (*ActVersion, error) {
	nodes, err := avq.Limit(2).All(setContextOp(ctx, avq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{actversion.Label}
	default:
		return nil, &NotSingularError{actversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (avq *ActVersionQuery) OnlyX(ctx context.Context) *ActVersion {
	node, err := avq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ActVersion ID in the query.
// Returns a *NotSingularError when more than one ActVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (avq *ActVersionQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = avq.Limit(2).IDs(setContextOp(ctx, avq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{actversion.Label}
	default:
		err = &NotSingularError{actversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (avq *ActVersionQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := avq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ActVersions.
func (avq *ActVersionQuery) All(ctx context.Context) ([]*ActVersion, error) {
	ctx = setContextOp(ctx, avq.ctx, "All")
	if err := avq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ActVersion, *ActVersionQuery]()
	return withInterceptors[[]*ActVersion](ctx, avq, qr, avq.inters)
}

// AllX is like All, but panics if an error occurs.
func (avq *ActVersionQuery) AllX(ctx context.Context) []*ActVersion {
	nodes, err := avq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ActVersion IDs.
func (avq *ActVersionQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if avq.ctx.Unique == nil && avq.path != nil {
		avq.Unique(true)
	}
	ctx = setContextOp(ctx, avq.ctx, "IDs")
	if err = avq.Select(actversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (avq *ActVersionQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := avq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (avq *ActVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, avq.ctx, "Count")
	if err := avq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, avq, querierCount[*ActVersionQuery](), avq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (avq *ActVersionQuery) CountX(ctx context.Context) int {
	count, err := avq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (avq *ActVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, avq.ctx, "Exist")
	switch _, err := avq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (avq *ActVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := avq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ActVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (avq *ActVersionQuery) Clone() *ActVersionQuery {
	if avq == nil {
		return nil
	}
	return &ActVersionQuery{
		config:        avq.config,
		ctx:           avq.ctx.Clone(),
		order:         append([]actversion.OrderOption{}, avq.order...),
		inters:        append([]Interceptor{}, avq.inters...),
		predicates:    append([]predicate.ActVersion{}, avq.predicates...),
		withAct:       avq.withAct.Clone(),
		withActImages: avq.withActImages.Clone(),
		// clone intermediate query.
		sql:  avq.sql.Clone(),
		path: avq.path,
	}
}

// WithAct tells the query-builder to eager-load the nodes that are connected to
// the "act" edge. The optional arguments are used to configure the query builder of the edge.
func (avq *ActVersionQuery) WithAct(opts ...func(*ActQuery)) *ActVersionQuery {
	query := (&ActClient{config: avq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	avq.withAct = query
	return avq
}

// WithActImages tells the query-builder to eager-load the nodes that are connected to
// the "act_images" edge. The optional arguments are used to configure the query builder of the edge.
func (avq *ActVersionQuery) WithActImages(opts ...func(*ActImageQuery)) *ActVersionQuery {
	query := (&ActImageClient{config: avq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	avq.withActImages = query
	return avq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ActVersion.Query().
//		GroupBy(actversion.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (avq *ActVersionQuery) GroupBy(field string, fields ...string) *ActVersionGroupBy {
	avq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ActVersionGroupBy{build: avq}
	grbuild.flds = &avq.ctx.Fields
	grbuild.label = actversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.ActVersion.Query().
//		Select(actversion.FieldCode).
//		Scan(ctx, &v)
func (avq *ActVersionQuery) Select(fields ...string) *ActVersionSelect {
	avq.ctx.Fields = append(avq.ctx.Fields, fields...)
	sbuild := &ActVersionSelect{ActVersionQuery: avq}
	sbuild.label = actversion.Label
	sbuild.flds, sbuild.scan = &avq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ActVersionSelect configured with the given aggregations.
func (avq *ActVersionQuery) Aggregate(fns ...AggregateFunc) *ActVersionSelect {
	return avq.Select().Aggregate(fns...)
}

func (avq *ActVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range avq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, avq); err != nil {
				return err
			}
		}
	}
	for _, f := range avq.ctx.Fields {
		if !actversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if avq.path != nil {
		prev, err := avq.path(ctx)
		if err != nil {
			return err
		}
		avq.sql = prev
	}
	return nil
}

func (avq *ActVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ActVersion, error) {
	var (
		nodes       = []*ActVersion{}
		withFKs     = avq.withFKs
		_spec       = avq.querySpec()
		loadedTypes = [2]bool{
			avq.withAct != nil,
			avq.withActImages != nil,
		}
	)
	if avq.withAct != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, actversion.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ActVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ActVersion{config: avq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, avq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := avq.withAct; query != nil {
		if err := avq.loadAct(ctx, query, nodes, nil,
			func(n *ActVersion, e *Act) { n.Edges.Act = e }); err != nil {
			return nil, err
		}
	}
	if query := avq.withActImages; query != nil {
		if err := avq.loadActImages(ctx, query, nodes,
			func(n *ActVersion) { n.Edges.ActImages = []*ActImage{} },
			func(n *ActVersion, e *ActImage) { n.Edges.ActImages = append(n.Edges.ActImages, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (avq *ActVersionQuery) loadAct(ctx context.Context, query *ActQuery, nodes []*ActVersion, init func(*ActVersion), assign func(*ActVersion, *Act)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*ActVersion)
	for i := range nodes {
		if nodes[i].act_act_versions == nil {
			continue
		}
		fk := *nodes[i].act_act_versions
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(act.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "act_act_versions" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (avq *ActVersionQuery) loadActImages(ctx context.Context, query *ActImageQuery, nodes []*ActVersion, init func(*ActVersion), assign func(*ActVersion, *ActImage)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*ActVersion)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.ActImage(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(actversion.ActImagesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.act_version_act_images
		if fk == nil {
			return fmt.Errorf(`foreign-key "act_version_act_images" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "act_version_act_images" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (avq *ActVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := avq.querySpec()
	_spec.Node.Columns = avq.ctx.Fields
	if len(avq.ctx.Fields) > 0 {
		_spec.Unique = avq.ctx.Unique != nil && *avq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, avq.driver, _spec)
}

func (avq *ActVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(actversion.Table, actversion.Columns, sqlgraph.NewFieldSpec(actversion.FieldID, field.TypeUint64))
	_spec.From = avq.sql
	if unique := avq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if avq.path != nil {
		_spec.Unique = true
	}
	if fields := avq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, actversion.FieldID)
		for i := range fields {
			if fields[i] != actversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := avq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := avq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := avq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := avq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (avq *ActVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(avq.driver.Dialect())
	t1 := builder.Table(actversion.Table)
	columns := avq.ctx.Fields
	if len(columns) == 0 {
		columns = actversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if avq.sql != nil {
		selector = avq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if avq.ctx.Unique != nil && *avq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range avq.predicates {
		p(selector)
	}
	for _, p := range avq.order {
		p(selector)
	}
	if offset := avq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := avq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ActVersionGroupBy is the group-by builder for ActVersion entities.
type ActVersionGroupBy struct {
	selector
	build *ActVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (avgb *ActVersionGroupBy) Aggregate(fns ...AggregateFunc) *ActVersionGroupBy {
	avgb.fns = append(avgb.fns, fns...)
	return avgb
}

// Scan applies the selector query and scans the result into the given value.
func (avgb *ActVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, avgb.build.ctx, "GroupBy")
	if err := avgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActVersionQuery, *ActVersionGroupBy](ctx, avgb.build, avgb, avgb.build.inters, v)
}

func (avgb *ActVersionGroupBy) sqlScan(ctx context.Context, root *ActVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(avgb.fns))
	for _, fn := range avgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*avgb.flds)+len(avgb.fns))
		for _, f := range *avgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*avgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := avgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ActVersionSelect is the builder for selecting fields of ActVersion entities.
type ActVersionSelect struct {
	*ActVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (avs *ActVersionSelect) Aggregate(fns ...AggregateFunc) *ActVersionSelect {
	avs.fns = append(avs.fns, fns...)
	return avs
}

// Scan applies the selector query and scans the result into the given value.
func (avs *ActVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, avs.ctx, "Select")
	if err := avs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActVersionQuery, *ActVersionSelect](ctx, avs.ActVersionQuery, avs, avs.inters, v)
}

func (avs *ActVersionSelect) sqlScan(ctx context.Context, root *ActVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(avs.fns))
	for _, fn := range avs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*avs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := avs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
