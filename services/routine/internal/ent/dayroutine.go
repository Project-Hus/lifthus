// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"routine/internal/ent/dayroutine"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DayRoutine is the model entity for the DayRoutine schema.
type DayRoutine struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Day holds the value of the "day" field.
	Day int `json:"day,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DayRoutineQuery when eager-loading is set.
	Edges                        DayRoutineEdges `json:"edges"`
	program_release_day_routines *int64
	selectValues                 sql.SelectValues
}

// DayRoutineEdges holds the relations/edges for other nodes in the graph.
type DayRoutineEdges struct {
	// RoutineActs holds the value of the routine_acts edge.
	RoutineActs []*RoutineAct `json:"routine_acts,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RoutineActsOrErr returns the RoutineActs value or an error if the edge
// was not loaded in eager-loading.
func (e DayRoutineEdges) RoutineActsOrErr() ([]*RoutineAct, error) {
	if e.loadedTypes[0] {
		return e.RoutineActs, nil
	}
	return nil, &NotLoadedError{edge: "routine_acts"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DayRoutine) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dayroutine.FieldID, dayroutine.FieldDay:
			values[i] = new(sql.NullInt64)
		case dayroutine.ForeignKeys[0]: // program_release_day_routines
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DayRoutine fields.
func (dr *DayRoutine) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dayroutine.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			dr.ID = int64(value.Int64)
		case dayroutine.FieldDay:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field day", values[i])
			} else if value.Valid {
				dr.Day = int(value.Int64)
			}
		case dayroutine.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field program_release_day_routines", value)
			} else if value.Valid {
				dr.program_release_day_routines = new(int64)
				*dr.program_release_day_routines = int64(value.Int64)
			}
		default:
			dr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DayRoutine.
// This includes values selected through modifiers, order, etc.
func (dr *DayRoutine) Value(name string) (ent.Value, error) {
	return dr.selectValues.Get(name)
}

// QueryRoutineActs queries the "routine_acts" edge of the DayRoutine entity.
func (dr *DayRoutine) QueryRoutineActs() *RoutineActQuery {
	return NewDayRoutineClient(dr.config).QueryRoutineActs(dr)
}

// Update returns a builder for updating this DayRoutine.
// Note that you need to call DayRoutine.Unwrap() before calling this method if this DayRoutine
// was returned from a transaction, and the transaction was committed or rolled back.
func (dr *DayRoutine) Update() *DayRoutineUpdateOne {
	return NewDayRoutineClient(dr.config).UpdateOne(dr)
}

// Unwrap unwraps the DayRoutine entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dr *DayRoutine) Unwrap() *DayRoutine {
	_tx, ok := dr.config.driver.(*txDriver)
	if !ok {
		panic("ent: DayRoutine is not a transactional entity")
	}
	dr.config.driver = _tx.drv
	return dr
}

// String implements the fmt.Stringer.
func (dr *DayRoutine) String() string {
	var builder strings.Builder
	builder.WriteString("DayRoutine(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dr.ID))
	builder.WriteString("day=")
	builder.WriteString(fmt.Sprintf("%v", dr.Day))
	builder.WriteByte(')')
	return builder.String()
}

// DayRoutines is a parsable slice of DayRoutine.
type DayRoutines []*DayRoutine
