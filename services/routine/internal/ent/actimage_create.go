// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"routine/internal/ent/actimage"
	"routine/internal/ent/actversion"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ActImageCreate is the builder for creating a ActImage entity.
type ActImageCreate struct {
	config
	mutation *ActImageMutation
	hooks    []Hook
}

// SetOrder sets the "order" field.
func (aic *ActImageCreate) SetOrder(u uint) *ActImageCreate {
	aic.mutation.SetOrder(u)
	return aic
}

// SetSrc sets the "src" field.
func (aic *ActImageCreate) SetSrc(s string) *ActImageCreate {
	aic.mutation.SetSrc(s)
	return aic
}

// SetID sets the "id" field.
func (aic *ActImageCreate) SetID(u uint64) *ActImageCreate {
	aic.mutation.SetID(u)
	return aic
}

// SetActVersionID sets the "act_version" edge to the ActVersion entity by ID.
func (aic *ActImageCreate) SetActVersionID(id uint64) *ActImageCreate {
	aic.mutation.SetActVersionID(id)
	return aic
}

// SetActVersion sets the "act_version" edge to the ActVersion entity.
func (aic *ActImageCreate) SetActVersion(a *ActVersion) *ActImageCreate {
	return aic.SetActVersionID(a.ID)
}

// Mutation returns the ActImageMutation object of the builder.
func (aic *ActImageCreate) Mutation() *ActImageMutation {
	return aic.mutation
}

// Save creates the ActImage in the database.
func (aic *ActImageCreate) Save(ctx context.Context) (*ActImage, error) {
	return withHooks(ctx, aic.sqlSave, aic.mutation, aic.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (aic *ActImageCreate) SaveX(ctx context.Context) *ActImage {
	v, err := aic.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (aic *ActImageCreate) Exec(ctx context.Context) error {
	_, err := aic.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (aic *ActImageCreate) ExecX(ctx context.Context) {
	if err := aic.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (aic *ActImageCreate) check() error {
	if _, ok := aic.mutation.Order(); !ok {
		return &ValidationError{Name: "order", err: errors.New(`ent: missing required field "ActImage.order"`)}
	}
	if _, ok := aic.mutation.Src(); !ok {
		return &ValidationError{Name: "src", err: errors.New(`ent: missing required field "ActImage.src"`)}
	}
	if v, ok := aic.mutation.Src(); ok {
		if err := actimage.SrcValidator(v); err != nil {
			return &ValidationError{Name: "src", err: fmt.Errorf(`ent: validator failed for field "ActImage.src": %w`, err)}
		}
	}
	if _, ok := aic.mutation.ActVersionID(); !ok {
		return &ValidationError{Name: "act_version", err: errors.New(`ent: missing required edge "ActImage.act_version"`)}
	}
	return nil
}

func (aic *ActImageCreate) sqlSave(ctx context.Context) (*ActImage, error) {
	if err := aic.check(); err != nil {
		return nil, err
	}
	_node, _spec := aic.createSpec()
	if err := sqlgraph.CreateNode(ctx, aic.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = uint64(id)
	}
	aic.mutation.id = &_node.ID
	aic.mutation.done = true
	return _node, nil
}

func (aic *ActImageCreate) createSpec() (*ActImage, *sqlgraph.CreateSpec) {
	var (
		_node = &ActImage{config: aic.config}
		_spec = sqlgraph.NewCreateSpec(actimage.Table, sqlgraph.NewFieldSpec(actimage.FieldID, field.TypeUint64))
	)
	if id, ok := aic.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := aic.mutation.Order(); ok {
		_spec.SetField(actimage.FieldOrder, field.TypeUint, value)
		_node.Order = value
	}
	if value, ok := aic.mutation.Src(); ok {
		_spec.SetField(actimage.FieldSrc, field.TypeString, value)
		_node.Src = value
	}
	if nodes := aic.mutation.ActVersionIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   actimage.ActVersionTable,
			Columns: []string{actimage.ActVersionColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(actversion.FieldID, field.TypeUint64),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.act_version_act_images = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ActImageCreateBulk is the builder for creating many ActImage entities in bulk.
type ActImageCreateBulk struct {
	config
	builders []*ActImageCreate
}

// Save creates the ActImage entities in the database.
func (aicb *ActImageCreateBulk) Save(ctx context.Context) ([]*ActImage, error) {
	specs := make([]*sqlgraph.CreateSpec, len(aicb.builders))
	nodes := make([]*ActImage, len(aicb.builders))
	mutators := make([]Mutator, len(aicb.builders))
	for i := range aicb.builders {
		func(i int, root context.Context) {
			builder := aicb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ActImageMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, aicb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, aicb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = uint64(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, aicb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (aicb *ActImageCreateBulk) SaveX(ctx context.Context) []*ActImage {
	v, err := aicb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (aicb *ActImageCreateBulk) Exec(ctx context.Context) error {
	_, err := aicb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (aicb *ActImageCreateBulk) ExecX(ctx context.Context) {
	if err := aicb.Exec(ctx); err != nil {
		panic(err)
	}
}
