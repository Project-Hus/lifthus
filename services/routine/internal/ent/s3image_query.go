// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"routine/internal/ent/predicate"
	"routine/internal/ent/s3actimage"
	"routine/internal/ent/s3image"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// S3ImageQuery is the builder for querying S3Image entities.
type S3ImageQuery struct {
	config
	ctx             *QueryContext
	order           []s3image.OrderOption
	inters          []Interceptor
	predicates      []predicate.S3Image
	withS3ActImages *S3ActImageQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the S3ImageQuery builder.
func (sq *S3ImageQuery) Where(ps ...predicate.S3Image) *S3ImageQuery {
	sq.predicates = append(sq.predicates, ps...)
	return sq
}

// Limit the number of records to be returned by this query.
func (sq *S3ImageQuery) Limit(limit int) *S3ImageQuery {
	sq.ctx.Limit = &limit
	return sq
}

// Offset to start from.
func (sq *S3ImageQuery) Offset(offset int) *S3ImageQuery {
	sq.ctx.Offset = &offset
	return sq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sq *S3ImageQuery) Unique(unique bool) *S3ImageQuery {
	sq.ctx.Unique = &unique
	return sq
}

// Order specifies how the records should be ordered.
func (sq *S3ImageQuery) Order(o ...s3image.OrderOption) *S3ImageQuery {
	sq.order = append(sq.order, o...)
	return sq
}

// QueryS3ActImages chains the current query on the "s3_act_images" edge.
func (sq *S3ImageQuery) QueryS3ActImages() *S3ActImageQuery {
	query := (&S3ActImageClient{config: sq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(s3image.Table, s3image.FieldID, selector),
			sqlgraph.To(s3actimage.Table, s3actimage.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, s3image.S3ActImagesTable, s3image.S3ActImagesColumn),
		)
		fromU = sqlgraph.SetNeighbors(sq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first S3Image entity from the query.
// Returns a *NotFoundError when no S3Image was found.
func (sq *S3ImageQuery) First(ctx context.Context) (*S3Image, error) {
	nodes, err := sq.Limit(1).All(setContextOp(ctx, sq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{s3image.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sq *S3ImageQuery) FirstX(ctx context.Context) *S3Image {
	node, err := sq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first S3Image ID from the query.
// Returns a *NotFoundError when no S3Image ID was found.
func (sq *S3ImageQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = sq.Limit(1).IDs(setContextOp(ctx, sq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{s3image.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sq *S3ImageQuery) FirstIDX(ctx context.Context) int64 {
	id, err := sq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single S3Image entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one S3Image entity is found.
// Returns a *NotFoundError when no S3Image entities are found.
func (sq *S3ImageQuery) Only(ctx context.Context) (*S3Image, error) {
	nodes, err := sq.Limit(2).All(setContextOp(ctx, sq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{s3image.Label}
	default:
		return nil, &NotSingularError{s3image.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sq *S3ImageQuery) OnlyX(ctx context.Context) *S3Image {
	node, err := sq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only S3Image ID in the query.
// Returns a *NotSingularError when more than one S3Image ID is found.
// Returns a *NotFoundError when no entities are found.
func (sq *S3ImageQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = sq.Limit(2).IDs(setContextOp(ctx, sq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{s3image.Label}
	default:
		err = &NotSingularError{s3image.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sq *S3ImageQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := sq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of S3Images.
func (sq *S3ImageQuery) All(ctx context.Context) ([]*S3Image, error) {
	ctx = setContextOp(ctx, sq.ctx, "All")
	if err := sq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*S3Image, *S3ImageQuery]()
	return withInterceptors[[]*S3Image](ctx, sq, qr, sq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sq *S3ImageQuery) AllX(ctx context.Context) []*S3Image {
	nodes, err := sq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of S3Image IDs.
func (sq *S3ImageQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if sq.ctx.Unique == nil && sq.path != nil {
		sq.Unique(true)
	}
	ctx = setContextOp(ctx, sq.ctx, "IDs")
	if err = sq.Select(s3image.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sq *S3ImageQuery) IDsX(ctx context.Context) []int64 {
	ids, err := sq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sq *S3ImageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sq.ctx, "Count")
	if err := sq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sq, querierCount[*S3ImageQuery](), sq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sq *S3ImageQuery) CountX(ctx context.Context) int {
	count, err := sq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sq *S3ImageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sq.ctx, "Exist")
	switch _, err := sq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sq *S3ImageQuery) ExistX(ctx context.Context) bool {
	exist, err := sq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the S3ImageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sq *S3ImageQuery) Clone() *S3ImageQuery {
	if sq == nil {
		return nil
	}
	return &S3ImageQuery{
		config:          sq.config,
		ctx:             sq.ctx.Clone(),
		order:           append([]s3image.OrderOption{}, sq.order...),
		inters:          append([]Interceptor{}, sq.inters...),
		predicates:      append([]predicate.S3Image{}, sq.predicates...),
		withS3ActImages: sq.withS3ActImages.Clone(),
		// clone intermediate query.
		sql:  sq.sql.Clone(),
		path: sq.path,
	}
}

// WithS3ActImages tells the query-builder to eager-load the nodes that are connected to
// the "s3_act_images" edge. The optional arguments are used to configure the query builder of the edge.
func (sq *S3ImageQuery) WithS3ActImages(opts ...func(*S3ActImageQuery)) *S3ImageQuery {
	query := (&S3ActImageClient{config: sq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sq.withS3ActImages = query
	return sq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.S3Image.Query().
//		GroupBy(s3image.FieldKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sq *S3ImageQuery) GroupBy(field string, fields ...string) *S3ImageGroupBy {
	sq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &S3ImageGroupBy{build: sq}
	grbuild.flds = &sq.ctx.Fields
	grbuild.label = s3image.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//	}
//
//	client.S3Image.Query().
//		Select(s3image.FieldKey).
//		Scan(ctx, &v)
func (sq *S3ImageQuery) Select(fields ...string) *S3ImageSelect {
	sq.ctx.Fields = append(sq.ctx.Fields, fields...)
	sbuild := &S3ImageSelect{S3ImageQuery: sq}
	sbuild.label = s3image.Label
	sbuild.flds, sbuild.scan = &sq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a S3ImageSelect configured with the given aggregations.
func (sq *S3ImageQuery) Aggregate(fns ...AggregateFunc) *S3ImageSelect {
	return sq.Select().Aggregate(fns...)
}

func (sq *S3ImageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sq); err != nil {
				return err
			}
		}
	}
	for _, f := range sq.ctx.Fields {
		if !s3image.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sq.path != nil {
		prev, err := sq.path(ctx)
		if err != nil {
			return err
		}
		sq.sql = prev
	}
	return nil
}

func (sq *S3ImageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*S3Image, error) {
	var (
		nodes       = []*S3Image{}
		_spec       = sq.querySpec()
		loadedTypes = [1]bool{
			sq.withS3ActImages != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*S3Image).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &S3Image{config: sq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sq.withS3ActImages; query != nil {
		if err := sq.loadS3ActImages(ctx, query, nodes,
			func(n *S3Image) { n.Edges.S3ActImages = []*S3ActImage{} },
			func(n *S3Image, e *S3ActImage) { n.Edges.S3ActImages = append(n.Edges.S3ActImages, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sq *S3ImageQuery) loadS3ActImages(ctx context.Context, query *S3ActImageQuery, nodes []*S3Image, init func(*S3Image), assign func(*S3Image, *S3ActImage)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*S3Image)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(s3actimage.FieldImageID)
	}
	query.Where(predicate.S3ActImage(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(s3image.S3ActImagesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ImageID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "image_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (sq *S3ImageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sq.querySpec()
	_spec.Node.Columns = sq.ctx.Fields
	if len(sq.ctx.Fields) > 0 {
		_spec.Unique = sq.ctx.Unique != nil && *sq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sq.driver, _spec)
}

func (sq *S3ImageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(s3image.Table, s3image.Columns, sqlgraph.NewFieldSpec(s3image.FieldID, field.TypeInt64))
	_spec.From = sq.sql
	if unique := sq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sq.path != nil {
		_spec.Unique = true
	}
	if fields := sq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, s3image.FieldID)
		for i := range fields {
			if fields[i] != s3image.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sq *S3ImageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sq.driver.Dialect())
	t1 := builder.Table(s3image.Table)
	columns := sq.ctx.Fields
	if len(columns) == 0 {
		columns = s3image.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sq.sql != nil {
		selector = sq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sq.ctx.Unique != nil && *sq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sq.predicates {
		p(selector)
	}
	for _, p := range sq.order {
		p(selector)
	}
	if offset := sq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// S3ImageGroupBy is the group-by builder for S3Image entities.
type S3ImageGroupBy struct {
	selector
	build *S3ImageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sgb *S3ImageGroupBy) Aggregate(fns ...AggregateFunc) *S3ImageGroupBy {
	sgb.fns = append(sgb.fns, fns...)
	return sgb
}

// Scan applies the selector query and scans the result into the given value.
func (sgb *S3ImageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgb.build.ctx, "GroupBy")
	if err := sgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*S3ImageQuery, *S3ImageGroupBy](ctx, sgb.build, sgb, sgb.build.inters, v)
}

func (sgb *S3ImageGroupBy) sqlScan(ctx context.Context, root *S3ImageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sgb.fns))
	for _, fn := range sgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sgb.flds)+len(sgb.fns))
		for _, f := range *sgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// S3ImageSelect is the builder for selecting fields of S3Image entities.
type S3ImageSelect struct {
	*S3ImageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ss *S3ImageSelect) Aggregate(fns ...AggregateFunc) *S3ImageSelect {
	ss.fns = append(ss.fns, fns...)
	return ss
}

// Scan applies the selector query and scans the result into the given value.
func (ss *S3ImageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ss.ctx, "Select")
	if err := ss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*S3ImageQuery, *S3ImageSelect](ctx, ss.S3ImageQuery, ss, ss.inters, v)
}

func (ss *S3ImageSelect) sqlScan(ctx context.Context, root *S3ImageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ss.fns))
	for _, fn := range ss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
