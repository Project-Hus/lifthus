// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"lifthus-auth/ent/lifthussession"
	"lifthus-auth/ent/predicate"
	"lifthus-auth/ent/user"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// LifthusSessionQuery is the builder for querying LifthusSession entities.
type LifthusSessionQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.LifthusSession
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LifthusSessionQuery builder.
func (lsq *LifthusSessionQuery) Where(ps ...predicate.LifthusSession) *LifthusSessionQuery {
	lsq.predicates = append(lsq.predicates, ps...)
	return lsq
}

// Limit the number of records to be returned by this query.
func (lsq *LifthusSessionQuery) Limit(limit int) *LifthusSessionQuery {
	lsq.ctx.Limit = &limit
	return lsq
}

// Offset to start from.
func (lsq *LifthusSessionQuery) Offset(offset int) *LifthusSessionQuery {
	lsq.ctx.Offset = &offset
	return lsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lsq *LifthusSessionQuery) Unique(unique bool) *LifthusSessionQuery {
	lsq.ctx.Unique = &unique
	return lsq
}

// Order specifies how the records should be ordered.
func (lsq *LifthusSessionQuery) Order(o ...OrderFunc) *LifthusSessionQuery {
	lsq.order = append(lsq.order, o...)
	return lsq
}

// QueryUser chains the current query on the "user" edge.
func (lsq *LifthusSessionQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: lsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := lsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := lsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lifthussession.Table, lifthussession.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lifthussession.UserTable, lifthussession.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(lsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LifthusSession entity from the query.
// Returns a *NotFoundError when no LifthusSession was found.
func (lsq *LifthusSessionQuery) First(ctx context.Context) (*LifthusSession, error) {
	nodes, err := lsq.Limit(1).All(setContextOp(ctx, lsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lifthussession.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lsq *LifthusSessionQuery) FirstX(ctx context.Context) *LifthusSession {
	node, err := lsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LifthusSession ID from the query.
// Returns a *NotFoundError when no LifthusSession ID was found.
func (lsq *LifthusSessionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = lsq.Limit(1).IDs(setContextOp(ctx, lsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lifthussession.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lsq *LifthusSessionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := lsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LifthusSession entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LifthusSession entity is found.
// Returns a *NotFoundError when no LifthusSession entities are found.
func (lsq *LifthusSessionQuery) Only(ctx context.Context) (*LifthusSession, error) {
	nodes, err := lsq.Limit(2).All(setContextOp(ctx, lsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lifthussession.Label}
	default:
		return nil, &NotSingularError{lifthussession.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lsq *LifthusSessionQuery) OnlyX(ctx context.Context) *LifthusSession {
	node, err := lsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LifthusSession ID in the query.
// Returns a *NotSingularError when more than one LifthusSession ID is found.
// Returns a *NotFoundError when no entities are found.
func (lsq *LifthusSessionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = lsq.Limit(2).IDs(setContextOp(ctx, lsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lifthussession.Label}
	default:
		err = &NotSingularError{lifthussession.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lsq *LifthusSessionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := lsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LifthusSessions.
func (lsq *LifthusSessionQuery) All(ctx context.Context) ([]*LifthusSession, error) {
	ctx = setContextOp(ctx, lsq.ctx, "All")
	if err := lsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LifthusSession, *LifthusSessionQuery]()
	return withInterceptors[[]*LifthusSession](ctx, lsq, qr, lsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lsq *LifthusSessionQuery) AllX(ctx context.Context) []*LifthusSession {
	nodes, err := lsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LifthusSession IDs.
func (lsq *LifthusSessionQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if lsq.ctx.Unique == nil && lsq.path != nil {
		lsq.Unique(true)
	}
	ctx = setContextOp(ctx, lsq.ctx, "IDs")
	if err = lsq.Select(lifthussession.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lsq *LifthusSessionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := lsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lsq *LifthusSessionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lsq.ctx, "Count")
	if err := lsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lsq, querierCount[*LifthusSessionQuery](), lsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lsq *LifthusSessionQuery) CountX(ctx context.Context) int {
	count, err := lsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lsq *LifthusSessionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lsq.ctx, "Exist")
	switch _, err := lsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lsq *LifthusSessionQuery) ExistX(ctx context.Context) bool {
	exist, err := lsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LifthusSessionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lsq *LifthusSessionQuery) Clone() *LifthusSessionQuery {
	if lsq == nil {
		return nil
	}
	return &LifthusSessionQuery{
		config:     lsq.config,
		ctx:        lsq.ctx.Clone(),
		order:      append([]OrderFunc{}, lsq.order...),
		inters:     append([]Interceptor{}, lsq.inters...),
		predicates: append([]predicate.LifthusSession{}, lsq.predicates...),
		withUser:   lsq.withUser.Clone(),
		// clone intermediate query.
		sql:  lsq.sql.Clone(),
		path: lsq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (lsq *LifthusSessionQuery) WithUser(opts ...func(*UserQuery)) *LifthusSessionQuery {
	query := (&UserClient{config: lsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	lsq.withUser = query
	return lsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UID uuid.UUID `json:"uid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LifthusSession.Query().
//		GroupBy(lifthussession.FieldUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (lsq *LifthusSessionQuery) GroupBy(field string, fields ...string) *LifthusSessionGroupBy {
	lsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LifthusSessionGroupBy{build: lsq}
	grbuild.flds = &lsq.ctx.Fields
	grbuild.label = lifthussession.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UID uuid.UUID `json:"uid,omitempty"`
//	}
//
//	client.LifthusSession.Query().
//		Select(lifthussession.FieldUID).
//		Scan(ctx, &v)
func (lsq *LifthusSessionQuery) Select(fields ...string) *LifthusSessionSelect {
	lsq.ctx.Fields = append(lsq.ctx.Fields, fields...)
	sbuild := &LifthusSessionSelect{LifthusSessionQuery: lsq}
	sbuild.label = lifthussession.Label
	sbuild.flds, sbuild.scan = &lsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LifthusSessionSelect configured with the given aggregations.
func (lsq *LifthusSessionQuery) Aggregate(fns ...AggregateFunc) *LifthusSessionSelect {
	return lsq.Select().Aggregate(fns...)
}

func (lsq *LifthusSessionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lsq); err != nil {
				return err
			}
		}
	}
	for _, f := range lsq.ctx.Fields {
		if !lifthussession.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lsq.path != nil {
		prev, err := lsq.path(ctx)
		if err != nil {
			return err
		}
		lsq.sql = prev
	}
	return nil
}

func (lsq *LifthusSessionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LifthusSession, error) {
	var (
		nodes       = []*LifthusSession{}
		_spec       = lsq.querySpec()
		loadedTypes = [1]bool{
			lsq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LifthusSession).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LifthusSession{config: lsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := lsq.withUser; query != nil {
		if err := lsq.loadUser(ctx, query, nodes, nil,
			func(n *LifthusSession, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (lsq *LifthusSessionQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*LifthusSession, init func(*LifthusSession), assign func(*LifthusSession, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LifthusSession)
	for i := range nodes {
		if nodes[i].UID == nil {
			continue
		}
		fk := *nodes[i].UID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "uid" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (lsq *LifthusSessionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lsq.querySpec()
	_spec.Node.Columns = lsq.ctx.Fields
	if len(lsq.ctx.Fields) > 0 {
		_spec.Unique = lsq.ctx.Unique != nil && *lsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lsq.driver, _spec)
}

func (lsq *LifthusSessionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lifthussession.Table, lifthussession.Columns, sqlgraph.NewFieldSpec(lifthussession.FieldID, field.TypeUUID))
	_spec.From = lsq.sql
	if unique := lsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lsq.path != nil {
		_spec.Unique = true
	}
	if fields := lsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lifthussession.FieldID)
		for i := range fields {
			if fields[i] != lifthussession.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lsq *LifthusSessionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lsq.driver.Dialect())
	t1 := builder.Table(lifthussession.Table)
	columns := lsq.ctx.Fields
	if len(columns) == 0 {
		columns = lifthussession.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lsq.sql != nil {
		selector = lsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lsq.ctx.Unique != nil && *lsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lsq.predicates {
		p(selector)
	}
	for _, p := range lsq.order {
		p(selector)
	}
	if offset := lsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LifthusSessionGroupBy is the group-by builder for LifthusSession entities.
type LifthusSessionGroupBy struct {
	selector
	build *LifthusSessionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lsgb *LifthusSessionGroupBy) Aggregate(fns ...AggregateFunc) *LifthusSessionGroupBy {
	lsgb.fns = append(lsgb.fns, fns...)
	return lsgb
}

// Scan applies the selector query and scans the result into the given value.
func (lsgb *LifthusSessionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lsgb.build.ctx, "GroupBy")
	if err := lsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LifthusSessionQuery, *LifthusSessionGroupBy](ctx, lsgb.build, lsgb, lsgb.build.inters, v)
}

func (lsgb *LifthusSessionGroupBy) sqlScan(ctx context.Context, root *LifthusSessionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lsgb.fns))
	for _, fn := range lsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lsgb.flds)+len(lsgb.fns))
		for _, f := range *lsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LifthusSessionSelect is the builder for selecting fields of LifthusSession entities.
type LifthusSessionSelect struct {
	*LifthusSessionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lss *LifthusSessionSelect) Aggregate(fns ...AggregateFunc) *LifthusSessionSelect {
	lss.fns = append(lss.fns, fns...)
	return lss
}

// Scan applies the selector query and scans the result into the given value.
func (lss *LifthusSessionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lss.ctx, "Select")
	if err := lss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LifthusSessionQuery, *LifthusSessionSelect](ctx, lss.LifthusSessionQuery, lss, lss.inters, v)
}

func (lss *LifthusSessionSelect) sqlScan(ctx context.Context, root *LifthusSessionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lss.fns))
	for _, fn := range lss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
