// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"lifthus-auth/ent/lifthustoken"
	"lifthus-auth/ent/predicate"
	"lifthus-auth/ent/user"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// LifthusTokenQuery is the builder for querying LifthusToken entities.
type LifthusTokenQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.LifthusToken
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LifthusTokenQuery builder.
func (ltq *LifthusTokenQuery) Where(ps ...predicate.LifthusToken) *LifthusTokenQuery {
	ltq.predicates = append(ltq.predicates, ps...)
	return ltq
}

// Limit the number of records to be returned by this query.
func (ltq *LifthusTokenQuery) Limit(limit int) *LifthusTokenQuery {
	ltq.ctx.Limit = &limit
	return ltq
}

// Offset to start from.
func (ltq *LifthusTokenQuery) Offset(offset int) *LifthusTokenQuery {
	ltq.ctx.Offset = &offset
	return ltq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ltq *LifthusTokenQuery) Unique(unique bool) *LifthusTokenQuery {
	ltq.ctx.Unique = &unique
	return ltq
}

// Order specifies how the records should be ordered.
func (ltq *LifthusTokenQuery) Order(o ...OrderFunc) *LifthusTokenQuery {
	ltq.order = append(ltq.order, o...)
	return ltq
}

// QueryUser chains the current query on the "user" edge.
func (ltq *LifthusTokenQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: ltq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ltq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ltq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lifthustoken.Table, lifthustoken.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lifthustoken.UserTable, lifthustoken.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ltq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LifthusToken entity from the query.
// Returns a *NotFoundError when no LifthusToken was found.
func (ltq *LifthusTokenQuery) First(ctx context.Context) (*LifthusToken, error) {
	nodes, err := ltq.Limit(1).All(setContextOp(ctx, ltq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lifthustoken.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ltq *LifthusTokenQuery) FirstX(ctx context.Context) *LifthusToken {
	node, err := ltq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LifthusToken ID from the query.
// Returns a *NotFoundError when no LifthusToken ID was found.
func (ltq *LifthusTokenQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ltq.Limit(1).IDs(setContextOp(ctx, ltq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lifthustoken.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ltq *LifthusTokenQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ltq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LifthusToken entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LifthusToken entity is found.
// Returns a *NotFoundError when no LifthusToken entities are found.
func (ltq *LifthusTokenQuery) Only(ctx context.Context) (*LifthusToken, error) {
	nodes, err := ltq.Limit(2).All(setContextOp(ctx, ltq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lifthustoken.Label}
	default:
		return nil, &NotSingularError{lifthustoken.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ltq *LifthusTokenQuery) OnlyX(ctx context.Context) *LifthusToken {
	node, err := ltq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LifthusToken ID in the query.
// Returns a *NotSingularError when more than one LifthusToken ID is found.
// Returns a *NotFoundError when no entities are found.
func (ltq *LifthusTokenQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ltq.Limit(2).IDs(setContextOp(ctx, ltq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lifthustoken.Label}
	default:
		err = &NotSingularError{lifthustoken.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ltq *LifthusTokenQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ltq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LifthusTokens.
func (ltq *LifthusTokenQuery) All(ctx context.Context) ([]*LifthusToken, error) {
	ctx = setContextOp(ctx, ltq.ctx, "All")
	if err := ltq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LifthusToken, *LifthusTokenQuery]()
	return withInterceptors[[]*LifthusToken](ctx, ltq, qr, ltq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ltq *LifthusTokenQuery) AllX(ctx context.Context) []*LifthusToken {
	nodes, err := ltq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LifthusToken IDs.
func (ltq *LifthusTokenQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ltq.ctx.Unique == nil && ltq.path != nil {
		ltq.Unique(true)
	}
	ctx = setContextOp(ctx, ltq.ctx, "IDs")
	if err = ltq.Select(lifthustoken.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ltq *LifthusTokenQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ltq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ltq *LifthusTokenQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ltq.ctx, "Count")
	if err := ltq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ltq, querierCount[*LifthusTokenQuery](), ltq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ltq *LifthusTokenQuery) CountX(ctx context.Context) int {
	count, err := ltq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ltq *LifthusTokenQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ltq.ctx, "Exist")
	switch _, err := ltq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ltq *LifthusTokenQuery) ExistX(ctx context.Context) bool {
	exist, err := ltq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LifthusTokenQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ltq *LifthusTokenQuery) Clone() *LifthusTokenQuery {
	if ltq == nil {
		return nil
	}
	return &LifthusTokenQuery{
		config:     ltq.config,
		ctx:        ltq.ctx.Clone(),
		order:      append([]OrderFunc{}, ltq.order...),
		inters:     append([]Interceptor{}, ltq.inters...),
		predicates: append([]predicate.LifthusToken{}, ltq.predicates...),
		withUser:   ltq.withUser.Clone(),
		// clone intermediate query.
		sql:  ltq.sql.Clone(),
		path: ltq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ltq *LifthusTokenQuery) WithUser(opts ...func(*UserQuery)) *LifthusTokenQuery {
	query := (&UserClient{config: ltq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ltq.withUser = query
	return ltq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UID uuid.UUID `json:"uid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LifthusToken.Query().
//		GroupBy(lifthustoken.FieldUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ltq *LifthusTokenQuery) GroupBy(field string, fields ...string) *LifthusTokenGroupBy {
	ltq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LifthusTokenGroupBy{build: ltq}
	grbuild.flds = &ltq.ctx.Fields
	grbuild.label = lifthustoken.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UID uuid.UUID `json:"uid,omitempty"`
//	}
//
//	client.LifthusToken.Query().
//		Select(lifthustoken.FieldUID).
//		Scan(ctx, &v)
func (ltq *LifthusTokenQuery) Select(fields ...string) *LifthusTokenSelect {
	ltq.ctx.Fields = append(ltq.ctx.Fields, fields...)
	sbuild := &LifthusTokenSelect{LifthusTokenQuery: ltq}
	sbuild.label = lifthustoken.Label
	sbuild.flds, sbuild.scan = &ltq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LifthusTokenSelect configured with the given aggregations.
func (ltq *LifthusTokenQuery) Aggregate(fns ...AggregateFunc) *LifthusTokenSelect {
	return ltq.Select().Aggregate(fns...)
}

func (ltq *LifthusTokenQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ltq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ltq); err != nil {
				return err
			}
		}
	}
	for _, f := range ltq.ctx.Fields {
		if !lifthustoken.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ltq.path != nil {
		prev, err := ltq.path(ctx)
		if err != nil {
			return err
		}
		ltq.sql = prev
	}
	return nil
}

func (ltq *LifthusTokenQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LifthusToken, error) {
	var (
		nodes       = []*LifthusToken{}
		_spec       = ltq.querySpec()
		loadedTypes = [1]bool{
			ltq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LifthusToken).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LifthusToken{config: ltq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ltq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ltq.withUser; query != nil {
		if err := ltq.loadUser(ctx, query, nodes, nil,
			func(n *LifthusToken, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ltq *LifthusTokenQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*LifthusToken, init func(*LifthusToken), assign func(*LifthusToken, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LifthusToken)
	for i := range nodes {
		fk := nodes[i].UID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "uid" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ltq *LifthusTokenQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ltq.querySpec()
	_spec.Node.Columns = ltq.ctx.Fields
	if len(ltq.ctx.Fields) > 0 {
		_spec.Unique = ltq.ctx.Unique != nil && *ltq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ltq.driver, _spec)
}

func (ltq *LifthusTokenQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lifthustoken.Table, lifthustoken.Columns, sqlgraph.NewFieldSpec(lifthustoken.FieldID, field.TypeUUID))
	_spec.From = ltq.sql
	if unique := ltq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ltq.path != nil {
		_spec.Unique = true
	}
	if fields := ltq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lifthustoken.FieldID)
		for i := range fields {
			if fields[i] != lifthustoken.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ltq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ltq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ltq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ltq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ltq *LifthusTokenQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ltq.driver.Dialect())
	t1 := builder.Table(lifthustoken.Table)
	columns := ltq.ctx.Fields
	if len(columns) == 0 {
		columns = lifthustoken.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ltq.sql != nil {
		selector = ltq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ltq.ctx.Unique != nil && *ltq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ltq.predicates {
		p(selector)
	}
	for _, p := range ltq.order {
		p(selector)
	}
	if offset := ltq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ltq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LifthusTokenGroupBy is the group-by builder for LifthusToken entities.
type LifthusTokenGroupBy struct {
	selector
	build *LifthusTokenQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ltgb *LifthusTokenGroupBy) Aggregate(fns ...AggregateFunc) *LifthusTokenGroupBy {
	ltgb.fns = append(ltgb.fns, fns...)
	return ltgb
}

// Scan applies the selector query and scans the result into the given value.
func (ltgb *LifthusTokenGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ltgb.build.ctx, "GroupBy")
	if err := ltgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LifthusTokenQuery, *LifthusTokenGroupBy](ctx, ltgb.build, ltgb, ltgb.build.inters, v)
}

func (ltgb *LifthusTokenGroupBy) sqlScan(ctx context.Context, root *LifthusTokenQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ltgb.fns))
	for _, fn := range ltgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ltgb.flds)+len(ltgb.fns))
		for _, f := range *ltgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ltgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ltgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LifthusTokenSelect is the builder for selecting fields of LifthusToken entities.
type LifthusTokenSelect struct {
	*LifthusTokenQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lts *LifthusTokenSelect) Aggregate(fns ...AggregateFunc) *LifthusTokenSelect {
	lts.fns = append(lts.fns, fns...)
	return lts
}

// Scan applies the selector query and scans the result into the given value.
func (lts *LifthusTokenSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lts.ctx, "Select")
	if err := lts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LifthusTokenQuery, *LifthusTokenSelect](ctx, lts.LifthusTokenQuery, lts, lts.inters, v)
}

func (lts *LifthusTokenSelect) sqlScan(ctx context.Context, root *LifthusTokenQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lts.fns))
	for _, fn := range lts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
