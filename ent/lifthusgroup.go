// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"lifthus-auth/ent/lifthusgroup"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// LifthusGroup is the model entity for the LifthusGroup schema.
type LifthusGroup struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LifthusGroup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case lifthusgroup.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type LifthusGroup", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LifthusGroup fields.
func (lg *LifthusGroup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case lifthusgroup.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			lg.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this LifthusGroup.
// Note that you need to call LifthusGroup.Unwrap() before calling this method if this LifthusGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (lg *LifthusGroup) Update() *LifthusGroupUpdateOne {
	return NewLifthusGroupClient(lg.config).UpdateOne(lg)
}

// Unwrap unwraps the LifthusGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (lg *LifthusGroup) Unwrap() *LifthusGroup {
	_tx, ok := lg.config.driver.(*txDriver)
	if !ok {
		panic("ent: LifthusGroup is not a transactional entity")
	}
	lg.config.driver = _tx.drv
	return lg
}

// String implements the fmt.Stringer.
func (lg *LifthusGroup) String() string {
	var builder strings.Builder
	builder.WriteString("LifthusGroup(")
	builder.WriteString(fmt.Sprintf("id=%v", lg.ID))
	builder.WriteByte(')')
	return builder.String()
}

// LifthusGroups is a parsable slice of LifthusGroup.
type LifthusGroups []*LifthusGroup
